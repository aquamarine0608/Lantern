{
  "name": "lantern-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for Lantern (mocked Kokoro TTS engine, real browser)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
